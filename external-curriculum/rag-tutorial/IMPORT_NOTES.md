# Import Notes

This directory contains the `viseshrp/rag-tutorial` wrapper repository without its nested `vendor/rag_tutorial` submodule content.

The original upstream tutorial source from that submodule is imported separately at:

```text
external-curriculum/rag-tutorial-source
```

This keeps all external sources consolidated as sibling curriculum sources instead of nesting one imported repository inside another.
